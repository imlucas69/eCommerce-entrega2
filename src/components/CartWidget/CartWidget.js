import cart from './assets/cart.svg'

const CartWidget = () => {
    return (
        <div>
            <img src={cart3} alt="cart-widget"/>
            0
        </div>
    )
}

export default CartWidget